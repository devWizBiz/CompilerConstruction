# Supported Instructions from x86-64

This document outlines the supported instructions for assembly generation.

## 1. **Instructions**
The following instructions are supported:

- `add` , `sub`, `mul`, `div`, `ret`, `mov`

